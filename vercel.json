{
  "buildCommand": "if [ -f lab/package.json ]; then npm --prefix lab install && npm --prefix lab run build && rm -rf dist && cp -r lab/dist dist; else npm install && npm run build; fi",
  "outputDirectory": "dist"
}
